#include<stdio.h>
int main(){
	int a[]={45,25,14,89,20},n,i,j,min,max;
	n=sizeof(a)/sizeof(a[0]);
	min=a[0];
	max=a[0];
	for(i=0;i<n;i++){
		if(min>a[i]){
			min=a[i];
		}
		if(max<a[i]){
			max=a[i];
		}
	}
	printf("%d maximum number\n",max);
	printf("%d minimum0 number\n",min);
	
}
